# Surface subset

The implemented surface is specified in [`docs/syntax.md`](../../docs/syntax.md). The Book narrates; `docs/` specifies. v0.1.0 includes `Nat`, `Unit`, functions, `case`, `measure`/`div`, multi-label effects, handlers, arithmetic prelude operators, and mutual top-level recursion.

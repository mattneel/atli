# Differential chain

```text
rules ↔ interpreter ↔ derive ↔ generator ↔ checker ↔ proofs ↔ surface ↔ native
```

Each arrow is pinned by goldens, property tests, proof statements, or compiled/oracle differentials.

# Adding examples

Add a header directive, run `atli test examples/`, and include tutorial samples with `{{#include}}`. Tutorial prose may not hand-type fenced Atli blocks; CI enforces this no-rot rule.

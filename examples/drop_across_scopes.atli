// expect: 9
effect A { op(x: Nat) -> Nat }
effect B { op(x: Nat) -> Nat }

fn fire(n: Nat) -> Nat = B.op(n)

fn main() -> Nat = handle (handle fire(1) {
  return(x) -> x
  A.op(p), k -> k(p)
}) {
  return(x) -> x
  B.op(p), _ -> 9
}
